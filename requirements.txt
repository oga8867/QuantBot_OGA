# =============================================================================
# 퀀트봇 의존성 (Phase 1~4 전체)
# =============================================================================
# 설치: pip install -r requirements.txt
# 선택적 패키지는 주석 해제하여 설치
# =============================================================================

# ─── Phase 1: 분석 엔진 (필수) ────────────────────────────────────────────

# 데이터 수집
yfinance>=0.2.36          # 미국 주가, 재무제표, 실적 데이터
pykrx>=1.0.45             # 한국 KOSPI/KOSDAQ 주가 데이터

# 수치 계산 & 데이터 처리
numpy>=1.24.0             # 수치 연산 (몬테카를로, 행렬 등)
pandas>=2.0.0             # 시계열 데이터프레임
scipy>=1.10.0             # 통계/최적화 함수

# 시각화 & 보고서
plotly>=5.18.0            # 인터랙티브 차트 (HTML 보고서)

# 유틸리티
requests>=2.31.0          # HTTP 요청
python-dateutil>=2.8.2    # 날짜 파싱
python-dotenv>=1.0.0      # .env 파일에서 API 키 로드

# ─── Phase 1 선택적 ───────────────────────────────────────────────────────
fredapi>=0.5.1            # FRED 거시경제 데이터 (API 키 필요)
feedparser>=6.0.10        # Google News RSS 뉴스 수집

# ─── Phase 2: 백테스트 + 전략 최적화 ──────────────────────────────────────
statsmodels>=0.14.0       # ARIMA/GARCH 시계열 모델
scikit-learn>=1.3.0       # ML 피처 엔지니어링, 교차검증
xgboost>=2.0.0            # XGBoost 예측 모델
optuna>=3.5.0             # 하이퍼파라미터 최적화 (베이지안)

# vectorbt는 별도 설치 권장 (의존성 충돌 가능):
# pip install vectorbt

# ─── Phase 3: 알림 시스템 ─────────────────────────────────────────────────
APScheduler>=3.10.0       # 작업 스케줄링 (정기 실행)

# python-telegram-bot은 비동기 버전이므로 별도 설치 권장:
# pip install python-telegram-bot>=20.7

# ─── Phase 4: 자동매매 ───────────────────────────────────────────────────
# alpaca-py>=0.21.0       # Alpaca 미국 주식 주문 (필요 시 해제)
websocket-client>=1.7.0   # 실시간 WebSocket 데이터

# ─── Phase 5: 웹 대시보드 ────────────────────────────────────────────────
flask>=3.0.0              # 웹 서버 프레임워크
flask-socketio>=5.3.0     # WebSocket 실시간 통신

# ─── Phase 6: LLM 뉴스 분석 (선택) ──────────────────────────────────────
# llama-cpp-python은 빌드가 필요할 수 있으므로 별도 설치 권장:
# pip install llama-cpp-python
# GPU 사용 시: CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python
